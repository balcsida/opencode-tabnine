{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode-tabnine",
  "version": "0.1.0",
  "description": "OpenCode plugin that exposes Tabnine Agentic as a provider",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun test && tsc --noEmit"
  },
  "keywords": [
    "opencode-plugin",
    "tabnine"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^24.12.2",
    "typescript": "^5.8.2"
  }
}
